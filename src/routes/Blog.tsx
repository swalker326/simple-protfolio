import { Typography } from "@mui/material";
import { DefaultLayout } from "../layouts";

export const Blog = () => {
  return (
    <DefaultLayout>
      <Typography typography={"h1"}>Blog</Typography>
    </DefaultLayout>
  );
};
